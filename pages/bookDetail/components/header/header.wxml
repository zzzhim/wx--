<view class="header">
    <image class="image" src="{{ detail.image }}"></image>
    <view class="title">{{ detail.title }}</view>
    <text class="name" wx:if="{{ detail.author !== undefined &&  detail.author.length > 0 }}">{{ detail.author[0] }}</text>
</view>