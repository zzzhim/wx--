<view class="block">
    <view class="title">{{ title }}</view>
    <view class="content">
        <text
            data-key="{{ item }}"
            wx:for="{{ content }}"
            wx:key="index"
            bindtap="handleClick">{{ item }}
        </text>
    </view>
</view>